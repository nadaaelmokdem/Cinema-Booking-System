using System.ComponentModel.DataAnnotations;

namespace CinemaBookingSystem.Models;

public class Hall
{
    public int Id { get; set; }

    [Required(ErrorMessage = "Hall name is required.")]
    [StringLength(50, ErrorMessage = "Name cannot be longer than 50 characters.")]
    [Display(Name = "Hall Name")]
    public string Name { get; set; } = string.Empty;

    [Required(ErrorMessage = "Seat capacity is required.")]
    [Range(1, 2000, ErrorMessage = "Capacity must be between 1 and 2000 seats.")]
    [Display(Name = "Seat Capacity")]
    public int Capacity { get; set; }

    [Required(ErrorMessage = "Please choose a cinema.")]
    [Display(Name = "Cinema")]
    public int CinemaId { get; set; }
    public Cinema? Cinema { get; set; }

    public ICollection<Showtime> Showtimes { get; set; } = new List<Showtime>();
}
